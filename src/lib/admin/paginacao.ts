/**
 * Constantes partilhadas entre as consultas do backoffice e as páginas.
 *
 * Vivem fora dos ficheiros com `"use server"`, que só podem exportar
 * funções assíncronas.
 */

/** Quantos ficheiros a mediateca mostra de cada vez. */
export const POR_PAGINA = 60;
